<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <CheckBox
        android:id="@+id/completed_checkBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_margin="5dp"
        android:button="@drawable/btn_check" />

    <TextView
        android:id="@+id/project_name_txtView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/completed_checkBox"
        android:layout_alignBottom="@+id/completed_checkBox"
        android:layout_toRightOf="@+id/completed_checkBox"
        android:text="@string/project_name"
        android:textColor="@android:color/black"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/project_description_txtView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/project_name_txtView"
        android:layout_marginTop="-5dp"
        android:layout_toRightOf="@+id/completed_checkBox"
        android:paddingBottom="7dp"
        android:text="@string/project_description"
        android:textColor="@android:color/black"/>

</RelativeLayout>